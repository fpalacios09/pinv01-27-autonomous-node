# Contribuir

No incluir credenciales, pesos o videos restringidos. Documentar Jetson, JetPack y versiones probadas.

```bash
python -m py_compile src/update_manager/node.py src/vehicle_counter/script.py examples/*.py
bash -n scripts/install/*.sh scripts/diagnostics/*.sh
```
